apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-cluster-health
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  cluster-health.json: |-
{{ .Files.Get "dashboards/cluster-health.json" | nindent 4 }}