apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-per-service-deep-dive
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  per-service-deep-dive.json: |-
{{ .Files.Get "dashboards/per-service-deep-dive.json" | nindent 4 }}