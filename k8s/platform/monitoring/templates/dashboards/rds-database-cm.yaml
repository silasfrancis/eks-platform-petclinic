apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-rds-database
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
data:
  rds-database.json: |-
{{ .Files.Get "dashboards/rds-database.json" | nindent 4 }}